/*
import {AnimationTriggerMetadata} from "@angular/core/src/animation/animation_metadata_wrapped";
import {AnimationMetadata} from "@angular/core/src/animation/animation_metadata_wrapped";

export class Trigger extends AnimationTriggerMetadata {
    constructor(private name: string, private animationMetaData: AnimationMetadata[]) {
        
    }
    
}
*/
